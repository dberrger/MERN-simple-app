import React from 'react';

export default class Cabinet extends React.Component {
    state = {  }
    render() {
        return (
            <div>This is a private route Cabinet, your name is {} </div>
        );
    }
}

// /export default connect(, )(Cabinet);